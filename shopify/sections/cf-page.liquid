{% comment %} sections/cf-page.liquid — Generic page {% endcomment %}
<div class="mx-auto max-w-[800px] px-4 py-12">
  <h1 class="text-3xl font-black text-ink-main mb-6">{{ page.title }}</h1>
  <div class="prose-cf text-ink-muted leading-relaxed">
    {{ page.content }}
  </div>
</div>
{% schema %}{ "name": "CF Page", "settings": [] }{% endschema %}
