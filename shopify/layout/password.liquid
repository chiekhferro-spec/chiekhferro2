{% comment %}
  layout/password.liquid
  CHIEKH FERRO — Minimal layout for the password (coming soon) page.
{% endcomment %}
<!doctype html>
<html lang="{{ request.locale.iso_code }}" dir="rtl">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ shop.name }}</title>
  {{ 'cf-tailwind.css' | asset_url | stylesheet_tag }}
  {{ content_for_header }}
</head>
<body class="min-h-screen bg-bg-primary text-ink-main antialiased flex items-center justify-center">
  {{ content_for_layout }}
</body>
</html>
